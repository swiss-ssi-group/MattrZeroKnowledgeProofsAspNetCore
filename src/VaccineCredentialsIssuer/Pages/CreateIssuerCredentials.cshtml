@page
@model VaccineCredentialsIssuer.Pages.AdminModel
@{
}

<div class="container-fluid">
    <div class="row">
        <div class="col-sm">
            <form method="post">
                <div>
                    <div class="form-group">
                        <label class="control-label">Name</label>
                        <input asp-for="IssuerCredential.CredentialName" class="form-control" />
                        <span asp-validation-for="IssuerCredential.CredentialName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        @if (Model.CreatingVaccinationData)
                        {
                            <input class="form-control"
                                   type="submit" readonly="@Model.CreatingVaccinationData"
                                   value="Create Credential Issuer" />
                        }

                    </div>
                    <div class="form-group">
                        @if (!Model.CreatingVaccinationData)
                        {
                            <div class="alert alert-success">
                                <strong>OIDC credential Issuer created</strong>
                            </div>
                        }

                        
                    </div>
                </div>
            </form>
            <hr />
            <p>When the callback is created, add this to the OIDC callback</p>
        </div>
        <div class="col-sm">
            <div>
                <img src="~/pexels-artem-podrez-5878503.jpg" width="200" alt="Vaccination Data Issuer">
                <div>
                    <b>Vaccination Data Callback Credentials</b>
                    <hr />
                    <dl class="row">
                        <dt class="col-sm-4">Callback</dt>
                        <dd class="col-sm-8">
                            @Model.Callback
                        </dd>
                    </dl>
                </div>
            </div>
        </div>
    </div>
</div>


